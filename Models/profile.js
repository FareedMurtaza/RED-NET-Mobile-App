class Profile {
    constructor(id, profilePic, name, email, password, bloodGroup, phoneNo) {
        this.id = id,
            this.profilePic = profilePic,
            this.name = name,
            this.email = email,
            this.password = password,
            this.bloodGroup = bloodGroup,
            this.phoneNo = phoneNo
    }
}

export default Profile;